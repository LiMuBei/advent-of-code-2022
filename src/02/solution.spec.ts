import { calculateWinningPoints } from "./solution";

describe("Day 2", () => {
    it("should calculate the number of points following the guide", () => {
        const result = calculateWinningPoints();
        console.log(`Day 2/1: ${result}`);
    })
})